# Assets

This folder is reserved for project images, icons, screenshots and other static assets.

The current project does not require external images, so the application works without additional downloads.
